{
  "name": "denarixx-sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "Denarixx Sentinel - PR Copilot Guard GitHub Action",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf build dist",
    "build:ts": "tsc -p tsconfig.json",
    "build": "npm run clean && npm run build:ts && ncc build build/index.js -o dist -m"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "fast-glob": "^3.3.2",
    "js-yaml": "^4.1.0",
    "minimatch": "^9.0.5"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/minimatch": "^5.1.2",
    "@types/node": "^25.2.3",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.5.4"
  }
}
